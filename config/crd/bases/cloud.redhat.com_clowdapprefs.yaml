---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: clowdapprefs.cloud.redhat.com
spec:
  group: cloud.redhat.com
  names:
    kind: ClowdAppRef
    listKind: ClowdAppRefList
    plural: clowdapprefs
    singular: clowdappref
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .spec.envName
      name: Env
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClowdAppRef is the Schema for the clowdapprefs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClowdAppRefSpec defines the desired state of ClowdAppRef
            properties:
              deployments:
                description: A list of deployments that represent services on a different
                  cluster
                items:
                  description: ClowdAppRefDeployment represents a deployment within
                    a ClowdAppRef
                  properties:
                    apiPath:
                      description: 'Deprecated: Use APIPaths instead'
                      type: string
                    apiPaths:
                      description: APIPaths defines the API paths available on this
                        deployment
                      items:
                        type: string
                      type: array
                    hostname:
                      description: Hostname where the deployment is accessible
                      type: string
                    name:
                      description: Name of the deployment
                      type: string
                    port:
                      description: 'Port where the deployment is accessible (default:
                        8000)'
                      format: int32
                      type: integer
                    privatePort:
                      description: 'PrivatePort for internal service communication
                        (default: 10000)'
                      format: int32
                      type: integer
                    tlsPort:
                      description: 'TLSPort where the deployment is accessible via
                        TLS (default: 8443)'
                      format: int32
                      type: integer
                    tlsPrivatePort:
                      description: 'TLSPrivatePort for internal service communication
                        via TLS (default: 10443)'
                      format: int32
                      type: integer
                    web:
                      description: Web indicates if this deployment has a public web
                        service
                      type: boolean
                    webServices:
                      description: WebServices defines the web services configuration
                        for this deployment
                      properties:
                        private:
                          description: Private defines the private web service configuration
                          properties:
                            enabled:
                              description: Enabled indicates if the private web service
                                is enabled
                              type: boolean
                          type: object
                        public:
                          description: Public defines the public web service configuration
                          properties:
                            enabled:
                              description: Enabled indicates if the public web service
                                is enabled
                              type: boolean
                          type: object
                      type: object
                  required:
                  - hostname
                  - name
                  type: object
                type: array
              disabled:
                description: Disabled turns off this ClowdAppRef
                type: boolean
              envName:
                description: The name of the ClowdEnvironment resource that this ClowdAppRef
                  will be used in
                type: string
              remoteCluster:
                description: RemoteCluster defines information about the remote cluster
                  where the services are located
                properties:
                  environment:
                    description: Environment defines the environment of the remote
                      cluster (e.g., prod, stage)
                    type: string
                  name:
                    description: Name defines the name of the remote cluster
                    type: string
                  region:
                    description: Region defines the region of the remote cluster
                    type: string
                type: object
            required:
            - deployments
            - envName
            type: object
          status:
            description: ClowdAppRefStatus defines the observed state of ClowdAppRef
            properties:
              conditions:
                description: Conditions represents the latest available observations
                  of the ClowdAppRef's current state
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        Last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A human readable message indicating details about the transition.
                        This field may be empty.
                      type: string
                    reason:
                      description: |-
                        The reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              ready:
                description: Ready indicates if the ClowdAppRef is ready to be used
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
