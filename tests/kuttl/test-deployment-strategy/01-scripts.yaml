---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
- script: for i in {1..5}; do kubectl get namespace test-deployment-strategy && exit 0 || sleep 1; done; echo "Namespace not found"; exit 1
- script: kubectl apply -f pods.yaml --namespace=test-deployment-strategy 2>&1 | grep "Forbidden"
